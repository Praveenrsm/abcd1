<!--
  FILE: web-tech-examples.html
  DESCRIPTION: A demonstration page showing how jQuery, AJAX, SPA Lite, Kendo UI, DataTables, and modern JS libraries work.
  NOTES: This is an overview. In a real project, you should split JS and HTML into separate files.
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Web Tech Comparison</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css">
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/dayjs@1.11.10/dayjs.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
</head>
<body class="p-4">
    <h2>🧪 Web Technologies Examples</h2>

    <!-- jQuery Example -->
    <h4>1. jQuery DOM Manipulation</h4>
    <button class="btn btn-primary" id="jqBtn">Click me</button>
    <p id="jqResult"></p>
    <script>
        $('#jqBtn').click(() => $('#jqResult').text('jQuery Click Worked!'));
    </script>

    <!-- AJAX Example -->
    <h4 class="mt-4">2. AJAX Request using jQuery</h4>
    <button class="btn btn-secondary" id="ajaxBtn">Get Dummy Data</button>
    <pre id="ajaxOutput"></pre>
    <script>
        $('#ajaxBtn').click(() => {
          $.ajax({
            url: 'https://jsonplaceholder.typicode.com/users/1',
            method: 'GET',
            success: (data) => {
              $('#ajaxOutput').text(JSON.stringify(data, null, 2));
            }
          });
        });
    </script>

    <!-- SPA Lite with Partial View using AJAX -->
    <h4 class="mt-4">3. SPA Lite (Load Partial with AJAX)</h4>
    <button class="btn btn-info" id="loadPartial">Load Partial</button>
    <div id="partialContainer" class="mt-2 border p-2"></div>
    <script>
        $('#loadPartial').click(() => {
          $('#partialContainer').load('/Partial/TaskList'); // Replace with actual controller URL
        });
    </script>

    <!-- DataTables Example -->
    <h4 class="mt-4">4. DataTables.js</h4>
    <table id="exampleTable" class="display">
        <thead>
            <tr><th>Name</th><th>Email</th></tr>
        </thead>
        <tbody>
            <tr><td>Jane Doe</td><td>jane@example.com</td></tr>
            <tr><td>John Smith</td><td>john@example.com</td></tr>
        </tbody>
    </table>
    <script>
        $(document).ready(() => $('#exampleTable').DataTable());
    </script>

    <!-- Client-side Form Enhancements -->
    <h4 class="mt-4">5. Client-Side Form Validation</h4>
    <form id="demoForm">
        <input type="text" name="username" class="form-control mb-2" placeholder="Enter name" required>
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
    <div id="formResult"></div>
    <script>
        $('#demoForm').submit(function (e) {
          e.preventDefault();
          $('#formResult').text('Form submitted successfully with name: ' + this.username.value);
        });
    </script>

    <!-- Alpine.js Example -->
    <h4 class="mt-4">6. Alpine.js Toggle</h4>
    <div x-data="{ open: false }">
        <button class="btn btn-warning" x-on:click="open = !open">Toggle</button>
        <div x-show="open" class="mt-2">Hello from Alpine.js!</div>
    </div>

    <!-- Axios Example -->
    <h4 class="mt-4">7. Axios AJAX</h4>
    <button class="btn btn-dark" id="axiosBtn">Load with Axios</button>
    <pre id="axiosResult"></pre>
    <script>
        document.getElementById('axiosBtn').addEventListener('click', () => {
          axios.get('https://jsonplaceholder.typicode.com/posts/1')
            .then(response => {
              document.getElementById('axiosResult').textContent = JSON.stringify(response.data, null, 2);
            });
        });
    </script>

    <!-- Day.js Example -->
    <h4 class="mt-4">8. Day.js Date Formatting</h4>
    <p id="dayjsTime"></p>
    <script>
        document.getElementById('dayjsTime').textContent = dayjs().format('YYYY-MM-DD HH:mm:ss');
    </script>

    <!-- Chart.js Example -->
    <h4 class="mt-4">9. Chart.js Bar Chart</h4>
    <canvas id="myChart" width="400" height="200"></canvas>
    <script>
        const ctx = document.getElementById('myChart');
        new Chart(ctx, {
          type: 'bar',
          data: {
            labels: ['Red', 'Blue', 'Yellow'],
            datasets: [{
              label: '# of Votes',
              data: [12, 19, 3],
              backgroundColor: ['red', 'blue', 'yellow']
            }]
          },
          options: { responsive: true, maintainAspectRatio: false }
        });
    </script>
</body>
</html>
